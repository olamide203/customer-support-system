import { type Option } from './categories';

const subjects: Option[] = [
    { id: 1, name: 'complaint' },
    { name: 'enquiry', id: 2 },
    { id: 3, name: 'request' },
];

export default subjects;
